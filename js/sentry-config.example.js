/**
 * Copia este archivo a js/sentry-config.js y rellena con tu DSN de Sentry.
 * js/sentry-config.js está en .gitignore — no subas el DSN real al repositorio.
 *
 * sentry.io → Proyecto → Settings → Client Keys (DSN)
 */
window.NIHONCHECK_SENTRY_CONFIG = {
  dsn: 'https://TU_CLAVE@o000000.ingest.sentry.io/0000000',
  environment: 'development',
};
